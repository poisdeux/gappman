## Makefile.am -- Process this file with automake to produce Makefile.in
SUBDIRS = src
dist_doc_DATA = README
EXTRA_DIST = xml-config-files/*.xml logos/*.png ChangeLog

dist-hook:
	find -name "*~" -exec rm {} \;